# ============================================================
# Diff Configuration
# ============================================================
# This file controls what the diff compares against.
# Priority: workflow inputs > this file > repository variables > defaults

# ------------------------------------------------------------
# REVISION_TO_COMPARE_COMMIT_ID: What version to compare against
# ------------------------------------------------------------
# Examples:
#   main                 Compare against main branch
#   release/5.0.0        Compare against a release tag
#   abc123def            Compare against a specific commit
#
REVISION_TO_COMPARE_COMMIT_ID=main

# ------------------------------------------------------------
# REVISION_TO_COMPARE_REPO_URL: External repository (optional)
# ------------------------------------------------------------
# Leave empty to compare within the same repository.
# Set to a GitHub URL to compare against a different repository.
#
# Example: https://github.com/OP-TED/ePO.git
#
REVISION_TO_COMPARE_REPO_URL=

# ------------------------------------------------------------
# Directory Settings
# ------------------------------------------------------------
# Root directory containing modules with OWL/SHACL files.
# OLD_ONTOLOGY_DIR: for the version being compared against
# NEW_ONTOLOGY_DIR: for the current (new) version
#
OLD_ONTOLOGY_DIR=implementation
NEW_ONTOLOGY_DIR=implementation

# ------------------------------------------------------------
# MODULES: Comma-separated list of modules to diff
# ------------------------------------------------------------
# Leave empty to auto-detect all modules in NEW_ONTOLOGY_DIR.
# Each module is a subdirectory containing owl_ontology/ and shacl_shapes/.
#
# Example: demo_ontology,demo_ontology_module
#
MODULES=

# ------------------------------------------------------------
# Output Settings
# ------------------------------------------------------------
RDF_DIFF_OUTDIR=diff-reports
